<!DOCTYPE html>
<html>
<head>
    <title>AliExpress Orders</title>
    <style>
        body {
            font-family: 'Segoe UI', Arial, sans-serif;
            background: #f7f7fa;
            margin: 0;
            padding: 0;
        }
        header {
            background: #00bcd4;
            color: #fff;
            padding: 1rem 2rem;
            text-align: center;
            font-size: 2rem;
            letter-spacing: 2px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.05);
        }
        .container {
            max-width: 900px;
            margin: 2rem auto;
            padding: 1rem;
        }
        .order-card {
            background: #fff;
            border-radius: 16px;
            border: 1px solid #e0e7ef;
            box-shadow: 0 2px 8px rgba(0,188,212,0.07);
            margin-bottom: 2rem;
            display: flex;
            align-items: center;
            padding: 1.5rem;
            transition: box-shadow 0.2s, border-color 0.2s;
        }
        .order-card:hover {
            box-shadow: 0 4px 16px rgba(25,118,210,0.12);
            border-color: #00bcd4;
        }
        .order-img {
            flex: 0 0 120px;
            margin-right: 2rem;
        }
        .order-img img {
            max-width: 100px;
            border-radius: 12px;
            box-shadow: 0 1px 4px rgba(0,0,0,0.08);
        }
        .order-info {
            flex: 1;
        }
        .order-info .order-name {
            font-size: 1rem;
            font-weight: normal;
            color: #333;
            margin-bottom: 0.5rem;
        }
        .order-info .order-meta {
            color: #555;
            margin-bottom: 0.5rem;
        }
        .order-info strong {
            color: #00bcd4;
        }
        .order-links {
            margin-top: 1rem;
        }
        .order-links a.details-link {
            background: #1976d2;
            color: #fff;
            border-radius: 6px;
            padding: 0.4em 1em;
            text-decoration: none;
            margin-right: 1rem;
            font-weight: bold;
            box-shadow: 0 1px 4px rgba(25,118,210,0.08);
            transition: background 0.2s;
        }
        .order-links a.details-link:hover {
            background: #1565c0;
        }
        .order-links a.aliexpress-link {
            background: #ff9800;
            color: #fff;
            border-radius: 6px;
            padding: 0.4em 1em;
            text-decoration: none;
            font-weight: bold;
            box-shadow: 0 1px 4px rgba(255,152,0,0.08);
            transition: background 0.2s;
        }
        .order-links a.aliexpress-link:hover {
            background: #f57c00;
        }
        footer {
            text-align: center;
            color: #00bcd4;
            padding: 2rem 0 1rem 0;
            font-size: 1rem;
        }
        @media (max-width: 600px) {
            .order-card {
                flex-direction: column;
                align-items: flex-start;
            }
            .order-img {
                margin-bottom: 1rem;
                margin-right: 0;
            }
        }
    </style>
</head>
<body>
    <header>AliExpress Orders</header>
    <div class="container">
        <% if (orders && orders.length) { %>
            <% orders.forEach(order => { %>
                <div class="order-card">
                    <div class="order-img">
                        <% if (order.imageUrl) { %>
                            <img src="<%= order.imageUrl %>" alt="Order image">
                        <% } %>
                    </div>
                    <div class="order-info">
                        <div class="order-name"><%= order.name %></div>
                        <div class="order-meta">
                            <div><strong>Date:</strong> <%= order.date %></div>
                            <div><strong>Price:</strong> <%= order.price %> <%= order.currency %></div>
                            <div><strong>Store:</strong> <%= order.storeName %></div>
                        </div>
                        <div class="order-links">
                            <a class="details-link" href="/orders/<%= order.orderId %>">Details</a>
                            <% if (order.detailUrl) { %>
                                <a class="aliexpress-link" href="<%= order.detailUrl %>" target="_blank">AliExpress</a>
                            <% } %>
                        </div>
                    </div>
                </div>
            <% }) %>
        <% } else { %>
            <p>No orders found.</p>
        <% } %>
    </div>
    <footer>
        &copy; <%= new Date().getFullYear() %> webnology gmbh
    </footer>
</body>
</html>